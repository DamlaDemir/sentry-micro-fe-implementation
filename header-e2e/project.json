{
  "name": "header-e2e",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "header-e2e/src",
  "targets": {
    "e2e": {
      "executor": "@nx/cypress:cypress",
      "options": {
        "cypressConfig": "header-e2e/cypress.config.ts",
        "testingType": "e2e",
        "devServerTarget": "header:serve"
      },
      "configurations": {
        "production": {
          "devServerTarget": "header:serve:production"
        },
        "ci": {
          "devServerTarget": "header:serve-static"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["header-e2e/**/*.{js,ts}"]
      }
    }
  },
  "implicitDependencies": ["header"],
  "tags": []
}
